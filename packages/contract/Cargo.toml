[package]
name = "beast-agent-registry"
version = "0.1.0"
edition = "2021"
description = "Beast AI Agent Registry — on-chain identity and reputation for autonomous AI agents on Casper Network"

[dependencies]
casper-contract = "2.1.0"
casper-types = "3.0.0"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1