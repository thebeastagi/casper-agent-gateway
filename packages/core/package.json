{
  "name": "@beast/casper-x402",
  "version": "0.1.0",
  "description": "x402 Payment Protocol v2 implementation for Casper Network — enables AI agents to pay for services on Casper",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --test test/*.test.js",
    "lint": "tsc --noEmit"
  },
  "keywords": ["x402", "casper", "payment", "ai-agent", "micropayments"],
  "license": "Apache-2.0",
  "dependencies": {
    "tweetnacl": "^1.0.3",
    "blakejs": "^1.2.1"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0"
  }
}